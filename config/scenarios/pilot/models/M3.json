{
  "scenarioId": "M3",
  "purpose": "Pilot Model Sweep - Llama 3.2 3B q4_k_m",
  "modelName": "llama-3.2-3b-instruct:q4_k_m",
  "outputSubdir": "M3_llama32_3b_q4km",
  "referenceBaselineId": "B0",
  "serverManifest": "infra/k8s/compositions/server/models/m3",
  "notes": "Model-only variation anchored to the official locked baseline B0. This scenario changes the model family member to the 3B q4_k_m variant while inheriting the fixed worker scenario, placement scenario, workload scenario, prompt, temperature, and request timeout directly from the baseline. The only intended experimental delta within the Model Sweep family is the resolved model and its derived server manifest."
}
